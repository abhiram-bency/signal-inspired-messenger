fastapi==0.115.6
uvicorn[standard]==0.32.1
# pydantic >=2.13 required: 2.10.x has no pre-built wheel for Python 3.14,
# causing pydantic-core compilation to fail (PyO3 0.22.6 does not support 3.14).
# Pinned to <3 so upgrades remain in v2.
pydantic>=2.13,<3
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Authentication (Phase 5)
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# Database (Phase 2)
sqlalchemy==2.0.36
aiosqlite==0.20.0

# WebSockets (Phase 10)
websockets==14.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Linting / type-checking (dev)
ruff==0.8.4
mypy==1.14.0
