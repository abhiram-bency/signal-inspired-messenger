"""
WebSocket package — Phase 10.

Will contain:
  manager.py   — ConnectionManager class (in-memory user→socket registry)
  handlers.py  — event router that dispatches incoming WS events
  events.py    — event type constants and WS event Pydantic schemas

Spec reference: WEBSOCKET_PROTOCOL §53, §54

Not implemented in Phase 0.
"""
