"""
Core package.

Contains:
  config      — application settings (Pydantic Settings)
  security    — JWT/password utilities (Phase 5)
  dependencies — FastAPI dependency injection (Phase 5)
  logging     — structured logging helpers
"""
